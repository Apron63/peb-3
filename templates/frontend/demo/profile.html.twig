{% extends 'frontend/base.html.twig' %}

{% block title %}Профиль{% endblock %}

{% block body %}

    {% include('frontend/components/header.html.twig') %}

    <div class="global-content">

        {% include('frontend/components/background.html.twig') %}

        <div class="wrapper container">
            {{ render(path('app_demo_left_menu', {'activeItem': 'my-programs'})) }}
            <div class="content">
                <div class="profile">
                    <div class="photo_block">
                        <div class="photo_wrapper">
                            <div class="photo_inner">
                                <img src="{{ asset('img/big.jpg') }}" alt="">
                            </div>
                        </div>
                        {# <form id="profileForm">
                            <div class="photo_change_button_wrapper">
                                <a class="photo_change_button" href="javascript:void(0)" id="btn-profile-upload">сменить фото</a>
                                <input type="file" name="image" id="profile-upload" style="display: none" data-url="{{ path('profile_upload_image') }}"/>
                                <p></p>
                                <div id="error-message" class="text-danger"></div>
                            </div>
                        </form> #}
                    </div>
                    <div class="data_block">
                        <div class="profile_row"><strong>Иванов</strong></div>
                        <div class="profile_row">Иван Иванович</div>
                        <div class="profile_row"><strong>Организация:</strong> ООО "Ромашка"</div>
                        <div class="profile_row"><strong>Должность:</strong>Должность</div>
                        <div class="profile_row"><strong>Логин:</strong>-</div>
                        <div class="profile_row"><strong>Пароль:</strong>-</div>
                        <div class="profile_row"><a class="profile_exit_link" href="{{ path('homepage')}}">Выйти из аккаунта</a></div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    {% include('frontend/components/footer.html.twig') %}

    <div class="overlay" popup-element="overlay"></div>

{% endblock %}